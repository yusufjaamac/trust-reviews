/*
const productlist = ({ props, title}) => {
  

  return (
    <div className="product-list">
      <h2>{ title }</h2>
      {products.map(product => (
        <div className="product-preview" key={product.id} >
          <h2>{ product.title }</h2>
          <p>Written by { product.author }</p>
        </div>
      ))}
    </div>
  );
}
 
export default productlist;
*/